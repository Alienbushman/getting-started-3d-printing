---
import '@fontsource/space-grotesk/600.css';
import '@fontsource/inter/400.css';
import '@fontsource/inter/500.css';
import '@fontsource/inter/600.css';

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const { title, description = 'Beginner-friendly 3D printing guides and printable models.', ogImage } = Astro.props;
const base = import.meta.env.BASE_URL;
const pathname = Astro.url.pathname;
const navActive = (segment: string) =>
  pathname.startsWith(`${base.replace(/\/$/, '')}/${segment}`) ? 'text-ink font-semibold' : '';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />
    <meta property="og:title" content={`${title} — getting-started-3d-printing`} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    {ogImage && <meta property="og:image" content={ogImage} />}
    {ogImage && <meta name="twitter:card" content="summary_large_image" />}
    {ogImage && <meta name="twitter:image" content={ogImage} />}
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <link rel="icon" href={`${base}favicon.ico`} />
    <title>{title} — getting-started-3d-printing</title>
    <slot name="head" />
  </head>
  <body class="bg font-sans text-ink">
    <a href="#main" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:bg-accent focus:text-white focus:px-3 focus:py-1 focus:rounded">
      Skip to content
    </a>

    <header class="border-b border-neutral-100">
      <div class="mx-auto max-w-content px-4 md:px-8 lg:px-12 flex items-center justify-between h-14">
        <a href={`${base}`} class="font-display font-semibold text-sm text-ink">
          getting-started-3d-printing
        </a>
        <nav aria-label="Site navigation">
          <ul class="flex gap-6 text-sm text-neutral-500">
            <li><a href={`${base}models/`} class={`hover:text-ink transition-colors ${navActive('models')}`}>Models</a></li>
            <li><a href={`${base}guide/`} class={`hover:text-ink transition-colors ${navActive('guide')}`}>Guide</a></li>
            <li><a href={`${base}glossary/`} class={`hover:text-ink transition-colors ${navActive('glossary')}`}>Glossary</a></li>
            <li><a href={`${base}search/`} class={`hover:text-ink transition-colors ${navActive('search')}`} aria-label="Search the site">Search</a></li>
            <li><a href={`${base}about/`} class={`hover:text-ink transition-colors ${navActive('about')}`}>About</a></li>
          </ul>
        </nav>
      </div>
    </header>

    <main id="main">
      <slot />
    </main>

    <footer class="border-t border-neutral-100 mt-16">
      <div class="mx-auto max-w-content px-4 md:px-8 lg:px-12 py-8 flex flex-wrap gap-4 items-center justify-between text-xs text-neutral-500">
        <span>alienbushman &copy; 2026</span>
        <nav aria-label="Footer navigation">
          <ul class="flex gap-4">
            <li><a href="https://github.com/Alienbushman" class="hover:text-ink">GitHub</a></li>
            <li><a href={`${base}about/`} class="hover:text-ink">License</a></li>
            <li><a href={`${base}models/`} class="hover:text-ink">Sitemap</a></li>
          </ul>
        </nav>
      </div>
    </footer>
  </body>
</html>
